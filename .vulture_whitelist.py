# Whitelist for vulture false positives

# Protocol method parameters (not actually unused, they define the interface)
path  # cytraco.bootstrap.AppConfig protocol methods
_.path  # Test mock implementations
